/**
 * HeroIllustration — removed per redesign spec.
 * Hero product composition is now inline in page.tsx using real product images.
 */
export function HeroDeviceArt() {
  return null;
}
export type HeroDevice = "dome" | "nvr" | "biometric" | "poe";
