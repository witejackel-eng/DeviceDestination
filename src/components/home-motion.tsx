/**
 * HomeMotion — decorative animation removed per redesign spec.
 * All decorative animations (anime.js, GSAP hero timelines) have been removed.
 * Motion/Framer Motion is still used for UI transitions (drawers, modals, cards).
 */
export function HomeMotion() {
  return null;
}
