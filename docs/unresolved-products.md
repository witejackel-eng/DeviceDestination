# Unresolved products

No generic or unclear product row has been published.

The source PDF described by the master specification was not attached to this execution context, so its stated 60 rows—including the two `[unclear]` model numbers—could not be inspected safely. When the file is available, place a normalized private CSV at `private-data/product-price-source.csv`, run `npm run products:import`, and review the generated ignored output. Exact models only may progress to research and publication.
